set forcekitty
set shell "/usr/bin/zsh"

preview kitten_images .png .gif .jpeg .jpg {{
    . ~/.zshrc
    kitten icat --clear --transfer-mode=stream --stdin=no --place "${w}x${h}@${x}x${y}" $f < /dev/null > /dev/tty && exit 1
}}

priority kitten_images
